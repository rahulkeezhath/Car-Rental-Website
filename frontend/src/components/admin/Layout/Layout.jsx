import React, { Fragment } from 'react'
import AdminRoute from '../../../routes/AdminRoute'

const Layout = () => {
  return <Fragment>
        <div>
            <AdminRoute/>
        </div>
    </Fragment>
}

export default Layout
